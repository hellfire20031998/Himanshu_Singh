import portfolio from './portfolio.json';

/** Site / brand (nav label, document title). */
export const site = portfolio.site;

/** Social profile URLs for the footer. */
export const SOCIAL = portfolio.social;

/** Bar chart colors keyed to skill categories (same order as categories in the chart). */
export const SKILL_CHART_COLORS = portfolio.skillChartColors;

/** Main resume content: hero, skills, jobs, projects, education. */
export const resumeData = portfolio.resume;
